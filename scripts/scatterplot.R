library(plotly)
library(stringr)

build_scatter_plot <- function() {
  scatter <- plot_ly(data = )
  
}

#Need scatter plot, the x is ALWAYS hate crimes rates
#Y axis is hate_crimes_formatted, without the hate crimes rates